package com.afour.hackthon.wiki.config;

//@Configuration
//@EnableWebMvc
public class WebConfig {/*implements WebMvcConfigurer{
	@Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
        .allowedOrigins("*")
		.allowedMethods("POST", "PUT", "GET", "DELETE", "HEAD", "OPTIONS")
		.allowedHeaders("Access-Control-Allow-Headers", "Content-Type", "Accept", "X-Requested-With", "remember-me")
		.exposedHeaders("Access-Control-Allow-Headers", "Content-Type", "Accept", "X-Requested-With", "remember-me")
		.allowCredentials(false).maxAge(3600);
    }*/
}
